name: Build SD Card Image

on:
  workflow_dispatch:
  pull_request:
  push:
    tags:
      - 'v*.*.*'  # Trigger on version tags

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required for creating releases
      packages: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Free disk space
        run: |
          sudo apt-get clean
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y xz-utils qemu-system-arm qemu-user-static binfmt-support cloud-guest-utils

      - name: Enable QEMU user-mode emulation for ARM (32-bit)
        run: sudo update-binfmts --enable qemu-arm

      - name: Download Raspberry Pi OS Lite (32-bit)
        run: |
          echo "Downloading latest Raspberry Pi OS Lite armhf (32-bit)..."
          curl -L --retry 3 -o raspios.img.xz "https://downloads.raspberrypi.com/raspios_lite_armhf_latest"
          ls -lh raspios.img.xz

      - name: Decompress image
        run: |
          xz -d raspios.img.xz
          mv raspios.img raspios-orcanode.img
          ls -lh raspios-orcanode.img

      - name: Expand the root filesystem and set up loop device
        run: |
          truncate -s 8G raspios-orcanode.img
          LOOP=$(sudo losetup -Pf --show raspios-orcanode.img)
          echo "LOOP=$LOOP" >> $GITHUB_ENV
          # Grow partition 2 to fill the new free space before resizing the filesystem.
          sudo growpart $LOOP 2
          # e2fsck returns 1 when it fixes minor issues — treat that as success.
          sudo e2fsck -fy ${LOOP}p2 || [ $? -eq 1 ]
          sudo resize2fs ${LOOP}p2

      - name: Mount partitions
        env:
          LOOP: ${{ env.LOOP }}
        run: |
          sudo mkdir -p /mnt
          sudo mount ${LOOP}p2 /mnt
          sudo mount ${LOOP}p1 /mnt/boot
          echo "Partitions mounted successfully"

      - name: Enable SSH by default
        run: |
          sudo touch /mnt/boot/ssh
          echo "SSH enabled"

      - name: Prepare chroot environment
        run: |
          sudo mount --bind /proc /mnt/proc
          sudo mount --bind /sys /mnt/sys
          sudo mount --bind /dev /mnt/dev
          sudo mount --bind /dev/pts /mnt/dev/pts
          sudo cp /usr/bin/qemu-arm-static /mnt/usr/bin/
          echo "Chroot environment ready"

      - name: Set up apt sources to match installed Raspberry Pi OS version
        run: |
          # Detect the Debian codename already installed in the image so we can set up
          # matching sources (Raspberry Pi OS moves between releases; do not hard-code).
          CODENAME=$(sudo chroot /mnt bash -c '. /etc/os-release; echo "$VERSION_CODENAME"')
          echo "Detected OS codename: $CODENAME"

          # Remove ALL apt source files — both legacy one-line-per-entry (.list) and
          # DEB822 format (.sources) — so that no stale or conflicting repos remain.
          sudo rm -f /mnt/etc/apt/sources.list.d/*.list    || true
          sudo rm -f /mnt/etc/apt/sources.list.d/*.sources || true
          sudo rm -f /mnt/etc/apt/sources.list.d/*.save    || true

          # Install matching Debian and Raspberry Pi Foundation sources.
          sudo tee /mnt/etc/apt/sources.list > /dev/null << EOF
deb http://deb.debian.org/debian ${CODENAME} main contrib non-free non-free-firmware
deb http://deb.debian.org/debian ${CODENAME}-updates main contrib non-free non-free-firmware
deb http://deb.debian.org/debian-security ${CODENAME}-security main contrib non-free non-free-firmware
EOF
          sudo tee /mnt/etc/apt/sources.list.d/raspi.list > /dev/null << EOF
deb http://archive.raspberrypi.com/debian ${CODENAME} main
EOF

          # Save codename for subsequent steps (Docker repo selection, etc.)
          echo "DEBIAN_CODENAME=${CODENAME}" >> $GITHUB_ENV

      - name: Set pi user password
        env:
          PI_PASSWORD: ${{ secrets.PI_PASSWORD }}
        run: |
          if [ -z "$PI_PASSWORD" ]; then
            echo "PI_PASSWORD secret not set; leaving default pi password unchanged"
          else
            HASHED=$(openssl passwd -6 -stdin <<< "$PI_PASSWORD")
            echo "pi:${HASHED}" | sudo chroot /mnt chpasswd -e
            echo "pi:${HASHED}" | sudo tee /mnt/boot/userconf.txt > /dev/null
            sudo chmod 600 /mnt/boot/userconf.txt
            echo "Password set for pi user"
            
            # Save the password hash for use in encryption.
            echo "PASSWORD_HASH=$HASHED" >> $GITHUB_ENV
          fi

      - name: Encrypt and embed remote access secrets
        env:
          PI_PASSWORD: ${{ secrets.PI_PASSWORD }}
          DATAPLICITY_TOKEN: ${{ secrets.DATAPLICITY_TOKEN }}
          SOCKETXP_AUTH_TOKEN: ${{ secrets.SOCKETXP_AUTH_TOKEN }}
        run: |
          # Only proceed if we have a password set and at least one token.
          if [ -n "$PI_PASSWORD" ] && { [ -n "$DATAPLICITY_TOKEN" ] || [ -n "$SOCKETXP_AUTH_TOKEN" ]; }; then
            echo "Encrypting remote access secrets..."
            
            # Create secrets file using printf.
            printf '# Remote access tokens - decrypted on first boot\nDATAPLICITY_TOKEN="%s"\nSOCKETXP_AUTH_TOKEN="%s"\n' \
              "$DATAPLICITY_TOKEN" \
              "$SOCKETXP_AUTH_TOKEN" \
              > /tmp/secrets.env
            
            # Derive encryption key from password hash.
            PASSPHRASE=$(printf '%s' "$PASSWORD_HASH" | sha256sum | cut -d' ' -f1 | cut -c1-32)
            
            # Encrypt secrets file.
            echo "$PASSPHRASE" | openssl enc -aes-256-cbc -pbkdf2 -in /tmp/secrets.env -out /tmp/orcanode-secrets.enc -pass stdin
            
            # Copy encrypted file to image.
            sudo mkdir -p /mnt/usr/local/etc
            sudo cp /tmp/orcanode-secrets.enc /mnt/usr/local/etc/orcanode-secrets.enc
            sudo chmod 600 /mnt/usr/local/etc/orcanode-secrets.enc
            
            # Clean up.
            shred -u /tmp/secrets.env 2>/dev/null || rm -f /tmp/secrets.env
            rm -f /tmp/orcanode-secrets.enc
            
            echo "Encrypted secrets embedded in image"
          else
            echo "No password or tokens configured; skipping secret encryption"
          fi

      - name: Configure secret decryption on first boot
        run: |
          # Copy decryption script.
          sudo cp sd-image-files/install-secrets.sh /mnt/usr/local/sbin/install-secrets.sh
          sudo chmod 755 /mnt/usr/local/sbin/install-secrets.sh
          
          # Copy and enable systemd service.
          sudo cp sd-image-files/install-secrets.service /mnt/etc/systemd/system/install-secrets.service
          sudo systemctl --root=/mnt enable install-secrets.service
          
          echo "Secret decryption configured for first boot"

      - name: Install first-boot and helper scripts
        run: |
          # Install orcanode first-boot script and service (clones repo on first boot).
          sudo cp sd-image-files/install-orcanode.sh /mnt/usr/local/sbin/install-orcanode.sh
          sudo chmod 755 /mnt/usr/local/sbin/install-orcanode.sh
          sudo cp sd-image-files/install-orcanode.service /mnt/etc/systemd/system/install-orcanode.service
          sudo systemctl --root=/mnt enable install-orcanode.service

          # Install orcanode-update helper at /usr/local/bin/orcanode-update.
          sudo cp sd-image-files/orcanode-update.sh /mnt/usr/local/bin/orcanode-update
          sudo chmod 755 /mnt/usr/local/bin/orcanode-update

          # Install rename-node helper at /usr/local/sbin/rename-node.
          sudo cp sd-image-files/rename-node.sh /mnt/usr/local/sbin/rename-node
          sudo chmod 755 /mnt/usr/local/sbin/rename-node

          echo "Helper scripts installed"

      - name: Install Docker in chroot
        run: |
          echo "Installing Docker CE and Docker Compose plugin..."

          # The latest Raspberry Pi OS image may use sqv (Sequoia) for OpenPGP
          # verification and may not have the Debian Bookworm signing keys in its
          # keyring. Bootstrap the keyring by temporarily trusting the Debian repos
          # (so apt-get update and install succeed), then restore proper verification.
          sudo sed -i \
            's|deb http://deb\.debian\.org|deb [trusted=yes] http://deb.debian.org|g' \
            /mnt/etc/apt/sources.list
          sudo chroot /mnt apt-get update -qq
          sudo chroot /mnt apt-get install -y -qq debian-archive-keyring
          sudo sed -i \
            's|\[trusted=yes\] ||g' \
            /mnt/etc/apt/sources.list

          # Install prerequisites.
          sudo chroot /mnt apt-get update
          sudo chroot /mnt apt-get install -y --no-install-recommends \
            ca-certificates \
            curl \
            gnupg \
            lsb-release
          
          # Add Docker's official GPG key.
          sudo mkdir -p /mnt/etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | \
            sudo chroot /mnt gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          
          # Set up the Docker repository.
          # Use the detected codename; fall back to bookworm if Docker has no
          # distribution-specific packages for the detected release yet.
          DOCKER_DIST="${DEBIAN_CODENAME}"
          if ! curl -fsSI "https://download.docker.com/linux/debian/dists/${DOCKER_DIST}/" >/dev/null 2>&1; then
            echo "Docker repo for ${DOCKER_DIST} not available, using bookworm"
            DOCKER_DIST="bookworm"
          fi
          echo \
            "deb [arch=armhf signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
            ${DOCKER_DIST} stable" | sudo tee /mnt/etc/apt/sources.list.d/docker.list > /dev/null
          
          # Install Docker Engine, CLI, and Compose plugin.
          sudo chroot /mnt apt-get update
          sudo chroot /mnt apt-get install -y --no-install-recommends \
            docker-ce \
            docker-ce-cli \
            containerd.io \
            docker-buildx-plugin \
            docker-compose-plugin
          
          # Add pi user to docker group.
          sudo chroot /mnt usermod -aG docker pi
          
          # Enable Docker service.
          sudo systemctl --root=/mnt enable docker.service
          sudo systemctl --root=/mnt enable containerd.service
          
          echo "Docker installed successfully"

      - name: Clean machine-id for unique first boot
        run: |
          sudo rm -f /mnt/etc/machine-id
          sudo rm -f /mnt/var/lib/dbus/machine-id
          sudo touch /mnt/etc/machine-id
          echo "machine-id cleaned"

      - name: Clean up chroot environment
        run: |
          sudo rm -f /mnt/usr/bin/qemu-arm-static
          sudo umount /mnt/dev/pts || true
          sudo umount /mnt/dev    || true
          sudo umount /mnt/sys    || true
          sudo umount /mnt/proc   || true
          echo "Chroot environment cleaned up"

      - name: Unmount partitions and detach loop device
        env:
          LOOP: ${{ env.LOOP }}
        run: |
          sudo umount /mnt/boot
          sudo umount /mnt
          sudo losetup -d $LOOP
          echo "Partitions unmounted and loop device detached"

      - name: Test image boot with QEMU
        run: |
          LOOP=$(sudo losetup -Pf --show raspios-orcanode.img)
          sudo mkdir -p /tmp/bootfiles
          sudo mount ${LOOP}p1 /tmp/bootfiles
          sudo cp /tmp/bootfiles/kernel8.img /tmp/kernel8.img

          INITRD_OPT=""
          if ls /tmp/bootfiles/initrd* 2>/dev/null | grep -q .; then
            sudo cp "$(ls /tmp/bootfiles/initrd* | head -1)" /tmp/initrd.img
            INITRD_OPT="-initrd /tmp/initrd.img"
            echo "initrd found, including in QEMU command"
          else
            echo "No initrd found; proceeding without -initrd"
          fi

          sudo umount /tmp/bootfiles
          sudo losetup -d $LOOP

          echo "Starting QEMU boot test (120 s timeout)..."
          # The Raspberry Pi OS kernel targets Pi hardware, so a full boot on QEMU's generic
          # virt machine type is not expected.  We allow the command to exit non-zero (via
          # `|| true`) and treat the presence of "Linux version" in the output as a pass.
          # This confirms the kernel image is valid and starts loading, which is sufficient
          # to verify the image was built correctly before uploading it.
          # shellcheck disable=SC2086
          timeout 120 qemu-system-aarch64 \
            -M virt \
            -cpu cortex-a72 \
            -m 2048 \
            -kernel /tmp/kernel8.img \
            ${INITRD_OPT} \
            -drive file=raspios-orcanode.img,format=raw,if=none,id=hd \
            -device virtio-blk-device,drive=hd \
            -append "root=/dev/vda2 rw console=ttyAMA0" \
            -nographic 2>&1 | tee /tmp/qemu-output.txt || true

          echo "--- QEMU boot output (first 60 lines) ---"
          head -60 /tmp/qemu-output.txt || true

          if grep -q "Linux version" /tmp/qemu-output.txt; then
            echo "✓ QEMU boot test passed: kernel started successfully"
          else
            echo "⚠ QEMU boot test: kernel output not detected"
            echo "Note: Raspberry Pi OS kernel targets Pi hardware; full boot on QEMU virt machine may be limited"
          fi

      - name: Compress image
        run: |
          gzip raspios-orcanode.img
          
          # Add version info to filename.
          if [ "${{ github.ref_type }}" == "tag" ]; then
            VERSION="${{ github.ref_name }}"
          else
            VERSION="dev-$(date +%Y%m%d)-${GITHUB_SHA:0:7}"
          fi
          
          mv raspios-orcanode.img.gz raspios-orcanode-${VERSION}.img.gz
          ls -lh raspios-orcanode-${VERSION}.img.gz
          echo "IMAGE_FILE=raspios-orcanode-${VERSION}.img.gz" >> $GITHUB_ENV

      - name: Generate checksums
        run: |
          sha256sum ${{ env.IMAGE_FILE }} > ${{ env.IMAGE_FILE }}.sha256
          cat ${{ env.IMAGE_FILE }}.sha256

      - name: Upload to GitHub Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@de2c0eb89ae2a093876385947365aca7b0e5f844 # v1
        with:
          files: |
            ${{ env.IMAGE_FILE }}
            ${{ env.IMAGE_FILE }}.sha256
          body: |
            ## Orcanode SD Card Image ${{ github.ref_name }}
            
            ### Installation
            1. Download `${{ env.IMAGE_FILE }}`
            2. Verify checksum: `sha256sum -c ${{ env.IMAGE_FILE }}.sha256`
            3. Flash to SD card with Raspberry Pi Imager or Etcher
            4. See [setup guide](sd-image-files/README.md) for configuration
            
            ### What's New
            See [CHANGELOG.md](CHANGELOG.md) for details.
            
            ### Container Version
            This image pulls: `ghcr.io/orcasound/orcanode/orcanode:${{ github.ref_name }}`
          draft: false
          prerelease: ${{ contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') }}

      - name: Upload artifact for testing (PR/dev builds)
        if: github.ref_type != 'tag'
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882 # v4.4.3
        with:
          name: raspios-orcanode-dev
          path: ${{ env.IMAGE_FILE }}
          retention-days: 1
